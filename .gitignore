# macOS
.DS_Store

# IDE / Editor
.vscode/
.idea/
*.swp

# Go build
bin/
/gcp-free-deploy
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# Go module cache (normally not committed)
vendor/

# Terraform
.terraform/
terraform.tfstate
terraform.tfstate.*
crash.log
crash.*.log
.gcp-free-deploy.tfplan
.gcp-free-deploy-destroy.tfplan
.gcp-free-deploy.lock
*.tfplan

# Terraform variable files (may contain sensitive values)
terraform.tfvars
*.tfvars
*.tfvars.json
.gcp-free-deploy.tfvars.json
.gcp-free-deploy.tfvars.json.tmp-*

# Local deployment config (personal/environment-specific values)
gcp-free-deploy.json

# GCP credential files
credentials.json
service-account*.json
*-credentials.json

# Environment, certificate, and tunnel credentials
.env
.env.*
!.env.example
*.pem
*.key
.cloudflared/

# Other logs
*.log
